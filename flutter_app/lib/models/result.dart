import 'package:flutter/rendering.dart';

class Result {
  String bmiLevel;
  String bmiLevelDescription;
  Color levelTextColor;

  Result(this.bmiLevel, this.bmiLevelDescription, this.levelTextColor);
}